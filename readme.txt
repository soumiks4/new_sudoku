run in jupyter notebook

running the following gives a grid with random numbers removed, true or false in the following line to indicate if the generated sudoku grid is solvable or not
generate_sudoku_problem()
